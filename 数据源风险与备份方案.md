# 数据源风险与备份方案 🛡️

## ⚠️ 当前风险评估（已优化）

### 数据源现状
**架构模式**: 多数据源自动故障转移  
**数据源数量**: 3个（2个API + 1个数据库）  
**状态**: ✅ 已实现高可用架构  
**风险等级**: 🟢 低风险

#### 数据源详情
1. **USA28 API**（主数据源，优先级1）
   - URL: `https://api.365kaik.com/api/v1/trend/getHistoryList`
   - 状态: ✅ 可用
   
2. **JND28 API**（备用数据源1，优先级2） ⭐ **新增**
   - URL: `https://c2api.canada28.vip/api/lotteryresult/result_jnd28`
   - 状态: ✅ 可用
   - 说明: 加拿大28数据源，自动故障转移
   
3. **Database**（备用数据源2，优先级99）
   - 位置: 本地数据库
   - 状态: ✅ 可用
   - 说明: 最后兜底，使用已缓存的历史数据

### 存在的风险

#### 1. 单点故障风险 🔴 高危
- **问题**: 只有1个数据源
- **影响**: 如果API失效，整个系统将无法获取开奖数据
- **概率**: 中等（第三方API不受控制）
- **后果**: 系统无法下注和结算

#### 2. 第三方API不可控 🟡 中危
- **问题**: API由第三方维护
- **可能变化**:
  - 改变URL地址
  - 增加认证要求（API Key等）
  - 限流或收费
  - 被防火墙屏蔽
  - 服务关闭
- **概率**: 较高（根据报告，3个数据源中已有2个失效）
- **后果**: 需要紧急修改代码

#### 3. SSL证书问题 🟡 中危
- **问题**: 当前代码忽略SSL证书验证
```typescript
const httpsAgent = new https.Agent({
  rejectUnauthorized: false, // 不安全
});
```
- **影响**: 存在安全隐患
- **后果**: 可能被中间人攻击

---

## 🛠️ 解决方案

### 方案1：建立备用数据源（推荐）⭐

#### 实施步骤

**Step 1: 寻找备用API**

可选的28彩票数据源：
1. **PC28官方API** （如果有）
2. **其他彩票平台API**
   - 北京28
   - 幸运28
   - 加拿大28（BCLC修复后）
3. **自建爬虫**
   - 爬取公开网站数据
   - 风险：反爬机制

**Step 2: 代码实现多数据源**

```typescript
// lottery.service.ts
export class LotteryService {
  private dataSources = [
    {
      name: 'USA28',
      priority: 1,
      url: 'https://api.365kaik.com/api/v1/trend/getHistoryList',
      parser: this.parseUSA28Data,
      enabled: true,
    },
    {
      name: 'PC28',  // 备用源1
      priority: 2,
      url: 'https://api.pc28.com/xxx',
      parser: this.parsePC28Data,
      enabled: true,
    },
    {
      name: 'LocalCache',  // 备用源2：本地缓存
      priority: 3,
      parser: this.parseLocalCache,
      enabled: true,
    },
  ];

  async syncLotteryData() {
    // 按优先级尝试每个数据源
    for (const source of this.dataSources.filter(s => s.enabled)) {
      try {
        console.log(`尝试数据源: ${source.name}`);
        const data = await this.fetchFromSource(source);
        if (data) {
          console.log(`✅ 成功从 ${source.name} 获取数据`);
          return data;
        }
      } catch (error) {
        console.error(`❌ 数据源 ${source.name} 失败:`, error.message);
        // 记录失败，继续下一个数据源
        await this.logDataSourceFailure(source.name, error);
        continue;
      }
    }
    
    throw new Error('所有数据源均失败');
  }
}
```

**Step 3: 配置管理**

在数据库中配置数据源：
```sql
CREATE TABLE lottery_data_sources (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50) NOT NULL,
  url VARCHAR(255) NOT NULL,
  priority INT DEFAULT 1,
  enabled BOOLEAN DEFAULT TRUE,
  last_success DATETIME,
  last_failure DATETIME,
  failure_count INT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 插入数据源配置
INSERT INTO lottery_data_sources (name, url, priority) VALUES
('USA28', 'https://api.365kaik.com/api/v1/trend/getHistoryList', 1),
('PC28', 'https://backup.api.com/lottery', 2),
('LocalCache', 'file:///data/lottery/cache', 3);
```

---

### 方案2：自建数据中转服务器 ⭐⭐

#### 架构设计

```
外部API → 中转服务器 → 你的系统
           (缓存+备份)
```

#### 优势
- ✅ 控制数据流
- ✅ 缓存历史数据
- ✅ 平滑切换数据源
- ✅ 监控和告警
- ✅ 数据备份

#### 实施方案

**1. 搭建中转服务**
```typescript
// data-relay-service/src/app.ts
import express from 'express';
import axios from 'axios';
import Redis from 'ioredis';

const app = express();
const redis = new Redis();

// 数据源配置
const sources = [
  { name: 'USA28', url: 'https://api.365kaik.com/...' },
  { name: 'Backup', url: 'https://backup.api.com/...' },
];

app.get('/api/lottery/latest', async (req, res) => {
  // 1. 先从缓存读取
  const cached = await redis.get('lottery:latest');
  if (cached) {
    return res.json(JSON.parse(cached));
  }

  // 2. 尝试从数据源获取
  for (const source of sources) {
    try {
      const data = await axios.get(source.url);
      // 缓存30秒
      await redis.setex('lottery:latest', 30, JSON.stringify(data.data));
      return res.json(data.data);
    } catch (error) {
      console.error(`Source ${source.name} failed`);
      continue;
    }
  }

  // 3. 所有源都失败，返回最后一次成功的数据
  const lastKnownGood = await redis.get('lottery:last_known_good');
  if (lastKnownGood) {
    return res.json(JSON.parse(lastKnownGood));
  }

  res.status(503).json({ error: 'All data sources unavailable' });
});

app.listen(3001);
```

**2. 修改主系统**
```typescript
// 改为调用自己的中转服务
const response = await axios.get('http://your-relay-server.com/api/lottery/latest');
```

---

### 方案3：本地数据库缓存+手动录入（最后手段）

#### 功能设计

```typescript
// lottery-manual.service.ts
export class LotteryManualService {
  /**
   * 手动录入开奖数据（当API全部失效时）
   */
  async manualInputLottery(data: {
    issue: string;
    number1: number;
    number2: number;
    number3: number;
    drawTime: string;
  }) {
    // 1. 验证管理员权限
    // 2. 验证数据格式
    // 3. 保存到数据库
    // 4. 触发结算流程
    
    return await this.prisma.lotteryResult.create({
      data: {
        issue: data.issue,
        number1: data.number1,
        number2: data.number2,
        number3: data.number3,
        resultSum: data.number1 + data.number2 + data.number3,
        drawTime: new Date(data.drawTime),
        // ... 其他字段
      },
    });
  }
}
```

#### 管理后台界面
```vue
<!-- ManualLotteryInput.vue -->
<template>
  <el-card title="手动录入开奖数据（紧急）">
    <el-form :model="form">
      <el-form-item label="期号">
        <el-input v-model="form.issue" placeholder="3364710" />
      </el-form-item>
      <el-form-item label="开奖号码">
        <el-input-number v-model="form.number1" :min="0" :max="9" />
        <el-input-number v-model="form.number2" :min="0" :max="9" />
        <el-input-number v-model="form.number3" :min="0" :max="9" />
      </el-form-item>
      <el-form-item label="开奖时间">
        <el-date-picker v-model="form.drawTime" type="datetime" />
      </el-form-item>
      <el-form-item>
        <el-button type="danger" @click="submitManual">
          紧急录入
        </el-button>
      </el-form-item>
    </el-form>
  </el-card>
</template>
```

---

### 方案4：监控和自动告警 ⭐

#### 实施方案

**1. 健康检查接口**
```typescript
// lottery.controller.ts
@Get('health')
async healthCheck() {
  const result = {
    timestamp: new Date(),
    status: 'healthy',
    dataSources: [],
    lastSuccessfulSync: null,
    warnings: [],
  };

  // 检查每个数据源
  for (const source of this.dataSources) {
    try {
      const test = await this.testDataSource(source);
      result.dataSources.push({
        name: source.name,
        status: 'ok',
        responseTime: test.responseTime,
      });
    } catch (error) {
      result.status = 'degraded';
      result.dataSources.push({
        name: source.name,
        status: 'failed',
        error: error.message,
      });
    }
  }

  // 检查最后一次成功同步时间
  const lastSync = await this.getLastSyncTime();
  result.lastSuccessfulSync = lastSync;
  
  if (Date.now() - lastSync.getTime() > 5 * 60 * 1000) {
    result.status = 'critical';
    result.warnings.push('超过5分钟未同步数据');
  }

  return result;
}
```

**2. 自动告警**
```typescript
// alert.service.ts
@Injectable()
export class AlertService {
  @Cron('*/5 * * * *')  // 每5分钟检查
  async checkDataSourceHealth() {
    const health = await this.lotteryService.healthCheck();
    
    if (health.status === 'critical') {
      // 发送告警
      await this.sendAlert({
        level: 'critical',
        message: '开奖数据源全部失效',
        details: health,
      });
    }
  }

  private async sendAlert(alert) {
    // 方式1: 邮件
    // await this.emailService.send(alert);
    
    // 方式2: 短信
    // await this.smsService.send(alert);
    
    // 方式3: 钉钉/微信通知
    // await this.dingTalkService.send(alert);
    
    // 方式4: 记录到数据库
    await this.prisma.alert.create({ data: alert });
  }
}
```

---

## 📊 风险矩阵

| 风险 | 概率 | 影响 | 等级 | 解决方案 |
|------|------|------|------|----------|
| USA28 API失效 | 中 | 高 | 🔴 高 | 方案1：备用数据源 |
| 网络不稳定 | 低 | 中 | 🟡 中 | 方案2：中转服务+缓存 |
| 需要手动干预 | 低 | 中 | 🟡 中 | 方案3：手动录入功能 |
| 无法及时发现问题 | 中 | 高 | 🔴 高 | 方案4：监控告警 |

---

## 🎯 推荐实施优先级

### ✅ 第一阶段（已完成）
- [x] ✅ 当前已使用USA28数据源
- [x] ✅ **已实施方案1：多数据源支持** ⭐
  - USA28（主数据源）
  - JND28（备用数据源1）
  - Database（备用数据源2）
- [x] ✅ 实现自动故障转移
- [x] ✅ 添加健康检查接口

### 🔄 第二阶段（进行中）
- [x] ✅ 已寻找到JND28备用数据源
- [x] ✅ 已实施多数据源支持
- [ ] 🔧 修复SSL证书验证问题（可选）
- [ ] 📊 实施方案4：监控和告警（建议）

### 🚀 第三阶段（未来优化）
- [ ] 🏗️ 考虑方案2：自建中转服务（可选）
- [ ] 🆘 实施方案3：手动录入功能（应急预案）
- [ ] 📈 数据源性能监控和统计
- [ ] 🔔 自动告警通知（邮件/短信）

---

## 💡 最佳实践建议

### 开发阶段
1. ✅ 使用当前USA28 API（已实现）
2. ✅ 添加详细日志（已实现）
3. ⚠️ 修复SSL证书验证
4. 📊 添加健康检查接口

### 测试阶段
1. 🧪 模拟API失败场景
2. 🔄 测试降级逻辑
3. ⏱️ 压力测试
4. 📈 监控响应时间

### 生产阶段
1. 🔔 设置告警（邮件/短信）
2. 📊 定期检查健康状态
3. 💾 保留历史数据备份
4. 📖 准备应急预案

---

## 🚨 应急预案

### 当USA28 API失效时

**立即行动**（0-5分钟）：
1. 检查错误日志
2. 尝试手动请求API
3. 检查网络连接

**短期措施**（5-30分钟）：
1. 切换到备用数据源（如果已准备）
2. 使用本地缓存数据
3. 暂停用户下注（避免无法结算）

**中期措施**（30分钟-2小时）：
1. 联系API提供方
2. 寻找替代数据源
3. 手动录入关键数据

**长期措施**（2小时+）：
1. 更换数据源
2. 修改代码并测试
3. 部署更新

---

## 📞 联系方式

**紧急情况联系**：
- 技术负责人: [电话]
- 备用联系人: [电话]
- API提供方: [联系方式]

---

---

## ✅ 快速验证

### 1. 检查数据源健康状态

```bash
# 管理员登录后访问
GET http://localhost:3000/api/lottery/data-source-health
```

预期响应：
```json
{
  "timestamp": "2025-11-29T...",
  "status": "healthy",
  "dataSources": [
    { "name": "USA28", "status": "ok", "priority": 1 },
    { "name": "JND28", "status": "ok", "priority": 2 },
    { "name": "Database", "status": "ok", "priority": 99 }
  ]
}
```

### 2. 测试自动故障转移

```bash
# 1. 正常情况（使用USA28）
npm run start:dev

# 观察日志
✅ 成功从 USA28 获取 2 条数据

# 2. 模拟USA28失败（断网或修改URL）
# 系统会自动切换到JND28
❌ USA28 失败: Network timeout
➡️ 切换到下一个数据源...
✅ 成功从 JND28 获取 2 条数据
```

### 3. 查看同步日志

```bash
# 查看实时同步日志
tail -f backend/logs/application.log | grep "数据源"

# 预期输出
🔍 尝试数据源: USA28 (优先级: 1)
✅ 成功从 USA28 获取 2 条数据 (245ms)
```

---

**文档版本**: v2.0  
**最后更新**: 2025-11-29  
**下次审查**: 建议每月审查一次  
**相关文档**: [多数据源自动切换方案.md](./多数据源自动切换方案.md)



