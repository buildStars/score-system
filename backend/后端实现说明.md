# ⚙️ 后端实现说明

## 📋 已完成的工作

### 1. ✅ 基础配置文件
- `.env.example` - 环境变量模板
- `package.json` - 项目依赖配置
- `tsconfig.json` - TypeScript配置
- `nest-cli.json` - NestJS CLI配置

### 2. ✅ 数据库配置
- `prisma/schema.prisma` - Prisma数据库模型（8张表）
- `prisma/seed.ts` - 数据库初始化种子数据

### 3. ✅ 核心文件
- `src/main.ts` - 应用入口文件
- `src/app.module.ts` - 根模块
- `src/prisma/prisma.service.ts` - Prisma数据库服务
- `src/prisma/prisma.module.ts` - Prisma模块

### 4. ✅ 公共组件
- `src/common/filters/http-exception.filter.ts` - 全局异常过滤器
- `src/common/interceptors/transform.interceptor.ts` - 响应格式拦截器

---

## 🚀 快速开始

### 第一步：安装依赖

```bash
cd score-system/backend
pnpm install
```

### 第二步：配置环境变量

复制 `.env.example` 为 `.env`并修改配置：

```env
DATABASE_URL="mysql://root:your_password@localhost:3306/score_system"
REDIS_HOST="localhost"
REDIS_PORT=6379
JWT_SECRET="your-secret-key-change-in-production"
PORT=3000
```

### 第三步：创建数据库

```bash
mysql -u root -p
CREATE DATABASE score_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

### 第四步：运行数据库迁移

```bash
# 生成Prisma Client
npx prisma generate

# 创建数据库表
npx prisma migrate dev --name init

# 初始化种子数据
npx prisma db seed
```

### 第五步：启动开发服务器

```bash
pnpm start:dev
```

访问：
- **API**: http://localhost:3000/api
- **Swagger文档**: http://localhost:3000/api-docs

---

## 📁 项目目录结构

```
backend/
├── prisma/
│   ├── schema.prisma          # ✅ 已创建 - 数据库模型
│   ├── seed.ts                # ✅ 已创建 - 种子数据
│   └── migrations/            # 迁移文件（自动生成）
│
├── src/
│   ├── common/                # 公共模块
│   │   ├── decorators/        # 📝 待创建 - 自定义装饰器
│   │   ├── filters/           
│   │   │   └── http-exception.filter.ts  # ✅ 已创建
│   │   ├── guards/            # 📝 待创建 - 守卫
│   │   ├── interceptors/      
│   │   │   └── transform.interceptor.ts  # ✅ 已创建
│   │   └── pipes/             # 📝 待创建 - 管道
│   │
│   ├── config/                # 📝 待创建 - 配置文件
│   │
│   ├── prisma/
│   │   ├── prisma.module.ts   # ✅ 已创建
│   │   └── prisma.service.ts  # ✅ 已创建
│   │
│   ├── modules/               # 业务模块
│   │   ├── auth/              # 📝 待创建 - 认证模块
│   │   ├── user/              # 📝 待创建 - 用户模块
│   │   ├── admin/             # 📝 待创建 - 管理员模块
│   │   ├── bet/               # 📝 待创建 - 下注模块
│   │   ├── lottery/           # 📝 待创建 - 开奖模块
│   │   ├── point/             # 📝 待创建 - 积分模块
│   │   └── system/            # 📝 待创建 - 系统模块
│   │
│   ├── app.module.ts          # ✅ 已创建 - 根模块
│   └── main.ts                # ✅ 已创建 - 入口文件
│
├── test/                      # 测试文件
├── .env.example               # ✅ 已创建 - 环境变量模板
├── .env                       # 📝 需手动创建
├── nest-cli.json              # ✅ 已创建
├── package.json               # ✅ 已创建
├── tsconfig.json              # ✅ 已创建
├── README.md                  # 项目说明
└── 后端实现说明.md            # ✅ 当前文档
```

---

## 📝 下一步需要创建的模块

### 1. 认证模块 (Auth Module)

**文件**：
- `src/modules/auth/auth.module.ts`
- `src/modules/auth/auth.controller.ts`
- `src/modules/auth/auth.service.ts`
- `src/modules/auth/strategies/jwt.strategy.ts`
- `src/modules/auth/strategies/local.strategy.ts`
- `src/modules/auth/dto/login.dto.ts`
- `src/common/guards/jwt-auth.guard.ts`
- `src/common/guards/roles.guard.ts`
- `src/common/decorators/current-user.decorator.ts`
- `src/common/decorators/roles.decorator.ts`

**功能**：
- 用户登录
- 管理员登录
- JWT Token生成与验证
- 登录日志记录

### 2. 用户模块 (User Module)

**文件**：
- `src/modules/user/user.module.ts`
- `src/modules/user/user.controller.ts`
- `src/modules/user/user.service.ts`
- `src/modules/user/dto/create-user.dto.ts`
- `src/modules/user/dto/update-user.dto.ts`
- `src/modules/user/dto/change-password.dto.ts`

**功能**：
- 获取用户信息
- 修改密码
- 用户列表（管理员）
- 创建用户（管理员）
- 调整积分（管理员）

### 3. 下注模块 (Bet Module)

**文件**：
- `src/modules/bet/bet.module.ts`
- `src/modules/bet/bet.controller.ts`
- `src/modules/bet/bet.service.ts`
- `src/modules/bet/dto/create-bet.dto.ts`
- `src/modules/bet/dto/query-bet.dto.ts`

**功能**：
- 提交下注
- 下注验证（积分、金额范围、次数限制）
- 下注历史查询
- 下注结算

### 4. 开奖模块 (Lottery Module)

**文件**：
- `src/modules/lottery/lottery.module.ts`
- `src/modules/lottery/lottery.controller.ts`
- `src/modules/lottery/lottery.service.ts`
- `src/modules/lottery/lottery.processor.ts` (BullMQ队列处理器)
- `src/modules/lottery/utils/lottery-rules.util.ts` (回本判定逻辑)

**功能**：
- 同步开奖数据（从旧系统）
- 开奖历史查询
- 当前期号查询
- 自动结算下注
- 回本判定逻辑

### 5. 积分模块 (Point Module)

**文件**：
- `src/modules/point/point.module.ts`
- `src/modules/point/point.controller.ts`
- `src/modules/point/point.service.ts`
- `src/modules/point/dto/query-point-record.dto.ts`

**功能**：
- 积分记录查询
- 积分统计
- 积分变动记录

### 6. 系统模块 (System Module)

**文件**：
- `src/modules/system/system.module.ts`
- `src/modules/system/system.controller.ts`
- `src/modules/system/system.service.ts`
- `src/modules/system/dto/update-bet-settings.dto.ts`
- `src/modules/system/dto/update-system-settings.dto.ts`

**功能**：
- 获取系统设置
- 更新系统设置
- 获取下注设置
- 更新下注设置

### 7. 管理员模块 (Admin Module)

**文件**：
- `src/modules/admin/admin.module.ts`
- `src/modules/admin/admin.controller.ts`
- `src/modules/admin/admin.service.ts`
- `src/modules/admin/dto/statistics-query.dto.ts`

**功能**：
- 统计数据
- 操作日志
- 数据清理

---

## 🎲 核心业务逻辑

### 回本判定逻辑 (lottery-rules.util.ts)

```typescript
// 判断是否回本
function isReturn(n1: number, n2: number, n3: number, sum: number): boolean {
  // 1. 对子或豹子
  if (n1 === n2 || n1 === n3 || n2 === n3) {
    return true;
  }
  
  // 2. 顺子
  if (isShunzi(n1, n2, n3)) {
    return true;
  }
  
  // 3. 总和13或14
  if (sum === 13 || sum === 14) {
    return true;
  }
  
  return false;
}

// 判断是否顺子
function isShunzi(n1: number, n2: number, n3: number): boolean {
  const arr = [n1, n2, n3].sort((a, b) => a - b);
  
  // 普通顺子
  if (arr[1] - arr[0] === 1 && arr[2] - arr[1] === 1) {
    return true;
  }
  
  // 特殊顺子: 0,8,9 或 0,1,9
  if (arr[0] === 0 && arr[1] === 8 && arr[2] === 9) return true;
  if (arr[0] === 0 && arr[1] === 1 && arr[2] === 9) return true;
  
  return false;
}
```

### 倍数下注结算逻辑

```typescript
// 计算倍数下注结果
function calculateMultipleBetResult(
  amount: number,
  isReturn: boolean,
  settings: BetSettings
): {
  fee: number;
  loss: number;
  resultAmount: number;
} {
  const fee = (amount / settings.multipleFeeBase) * settings.multipleFeeRate;
  
  if (isReturn) {
    // 回本：只扣手续费
    return {
      fee,
      loss: 0,
      resultAmount: amount - fee,
    };
  } else {
    // 不回本：损失80% + 手续费
    const loss = amount * settings.multipleLossRate;
    return {
      fee,
      loss,
      resultAmount: -(loss + fee),
    };
  }
}
```

### 组合下注结算逻辑

```typescript
// 判断组合是否中奖
function isComboBetWin(betContent: string, resultSum: number): boolean {
  const size = resultSum >= 14 ? '大' : '小';
  const oddEven = resultSum % 2 === 1 ? '单' : '双';
  
  switch (betContent) {
    case '大': return size === '大';
    case '小': return size === '小';
    case '单': return oddEven === '单';
    case '双': return oddEven === '双';
    case '大单': return size === '大' && oddEven === '单';
    case '大双': return size === '大' && oddEven === '双';
    case '小单': return size === '小' && oddEven === '单';
    case '小双': return size === '小' && oddEven === '双';
    default: return false;
  }
}

// 计算组合下注结果
function calculateComboBetResult(
  amount: number,
  isWin: boolean,
  settings: BetSettings
): {
  fee: number;
  resultAmount: number;
} {
  const fee = (amount / settings.comboFeeBase) * settings.comboFeeRate;
  
  if (isWin) {
    // 中奖：返还下注 - 手续费
    return {
      fee,
      resultAmount: amount - fee,
    };
  } else {
    // 不中奖：损失全部
    return {
      fee: 0,
      resultAmount: -amount,
    };
  }
}
```

---

## 🔐 认证流程

### JWT认证流程

```
1. 用户登录 → 验证用户名密码
2. 生成JWT Token（有效期7天）
3. 返回Token给前端
4. 前端请求时携带Token（Authorization: Bearer {token}）
5. JWT Guard验证Token
6. 从Token中提取用户信息
7. 将用户信息注入到Request对象
8. Controller通过@CurrentUser()装饰器获取用户信息
```

### 管理员认证流程

```
1. 管理员登录 → 验证账号密码
2. 生成JWT Token（有效期12小时）
3. 记录登录日志
4. 返回Token给前端
5. 后续请求验证Token + 验证角色权限
```

---

## 📊 数据库表说明

### 8张核心表

1. **users** - 用户表（存储用户信息和积分）
2. **admins** - 管理员表
3. **lottery_results** - 开奖结果表
4. **bets** - 下注记录表
5. **point_records** - 积分记录表
6. **bet_settings** - 下注设置表
7. **system_settings** - 系统设置表
8. **admin_logs** - 管理员操作日志表

详细设计请参考：[../../数据库设计文档.md](../../数据库设计文档.md)

---

## 🧪 测试

### 单元测试
```bash
pnpm test
```

### E2E测试
```bash
pnpm test:e2e
```

### 测试覆盖率
```bash
pnpm test:cov
```

---

## 📦 打包部署

```bash
# 打包
pnpm build

# 使用PM2启动
pm2 start dist/main.js --name score-api

# 查看日志
pm2 logs score-api

# 重启
pm2 restart score-api
```

---

## 🔧 常用命令

```bash
# Prisma相关
npx prisma studio              # 打开数据库可视化管理
npx prisma generate            # 生成Prisma Client
npx prisma migrate dev         # 创建迁移
npx prisma migrate deploy      # 部署迁移（生产环境）
npx prisma db seed             # 运行种子数据
npx prisma format              # 格式化schema文件

# 开发相关
pnpm start:dev                 # 启动开发服务器
pnpm start:debug               # 调试模式启动
pnpm lint                      # 代码检查
pnpm format                    # 代码格式化
```

---

## 📝 种子数据说明

运行 `npx prisma db seed` 后会自动创建：

1. **默认管理员**
   - 用户名：`admin`
   - 密码：`admin123`
   - 角色：superadmin

2. **测试用户**（仅开发环境）
   - user001 / 123456（积分5000）
   - user002 / 123456（积分3000）

3. **下注设置**（8条配置）
   - 倍数手续费比例：3
   - 组合手续费比例：5
   - 最小下注：10
   - 最大下注：10000
   - 等...

4. **系统设置**（5条配置）
   - 游戏开启状态：true
   - 维护模式：false
   - 系统公告
   - 数据源地址
   - 自动结算开关

---

## ✅ 当前进度

- [x] 项目基础配置
- [x] 数据库Schema设计
- [x] Prisma配置和种子数据
- [x] 主入口文件
- [x] 公共组件（过滤器、拦截器）
- [ ] 认证模块
- [ ] 用户模块
- [ ] 下注模块
- [ ] 开奖模块
- [ ] 积分模块
- [ ] 系统模块
- [ ] 管理员模块

---

## 🚀 后续计划

1. 创建认证模块（JWT策略、守卫、装饰器）
2. 创建用户模块（用户CRUD、密码管理）
3. 创建下注模块（下注提交、验证、查询）
4. 创建开奖模块（数据同步、自动结算）
5. 创建积分模块（记录查询、统计）
6. 创建系统模块（配置管理）
7. 创建管理员模块（统计、日志）
8. 编写单元测试
9. 编写E2E测试
10. 性能优化

---

**文档版本**：v1.0  
**创建日期**：2024年11月26日  
**最后更新**：2024年11月26日

---

## 📞 需要帮助？

如有问题，请查看：
1. [项目总览](../../计分系统-项目总览.md)
2. [数据库设计](../../数据库设计文档.md)
3. [API接口文档](../../API接口文档.md)
4. [业务规则详解](../../业务规则详解文档.md)



