# 🎯 计分系统 - 管理后台使用指南

## 📖 项目说明

这是一个基于 **Vue 3 + TypeScript + Element Plus** 开发的计分系统管理后台，提供完整的用户管理、开奖管理、下注管理、积分管理和系统设置功能。

---

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install --legacy-peer-deps
```

### 2. 启动开发服务器

```bash
npm run dev
```

项目将在 `http://localhost:5174` 启动

### 3. 打包生产版本

```bash
npm run build
```

### 4. 预览生产版本

```bash
npm run preview
```

---

## 📁 项目结构

```
frontend-admin/
├── public/                  # 公共静态资源
├── src/
│   ├── api/                # API 接口
│   │   ├── auth.ts        # 认证接口
│   │   ├── users.ts       # 用户接口
│   │   ├── lottery.ts     # 开奖接口
│   │   ├── bets.ts        # 下注接口
│   │   ├── points.ts      # 积分接口
│   │   ├── settings.ts    # 设置接口
│   │   └── statistics.ts  # 统计接口
│   │
│   ├── layout/            # 布局组件
│   │   └── MainLayout.vue # 主布局（侧边栏+顶栏）
│   │
│   ├── router/            # 路由配置
│   │   └── index.ts       # 路由定义和守卫
│   │
│   ├── stores/            # 状态管理
│   │   └── auth.ts        # 认证状态
│   │
│   ├── types/             # TypeScript 类型定义
│   │   └── index.ts       # 所有接口类型
│   │
│   ├── utils/             # 工具函数
│   │   ├── request.ts     # Axios 封装
│   │   └── format.ts      # 格式化工具
│   │
│   ├── views/             # 页面组件
│   │   ├── Login.vue      # 登录页
│   │   ├── Dashboard.vue  # 首页（统计）
│   │   ├── Users.vue      # 用户管理
│   │   ├── LotteryHistory.vue    # 开奖历史
│   │   ├── BetRecords.vue        # 下单记录
│   │   ├── PointRecords.vue      # 积分记录
│   │   ├── BetSettings.vue       # 模式设置
│   │   └── SystemSettings.vue    # 网站设置
│   │
│   ├── App.vue            # 根组件
│   └── main.ts            # 入口文件
│
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## 🎨 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.5+ | 渐进式 JavaScript 框架 |
| TypeScript | 5.7+ | JavaScript 的超集 |
| Vite | 6.0+ | 下一代前端构建工具 |
| Element Plus | 2.9+ | 基于 Vue 3 的组件库 |
| Vue Router | 4.5+ | Vue 官方路由管理器 |
| Pinia | 2.2+ | Vue 官方状态管理库 |
| Axios | 1.7+ | HTTP 客户端 |
| ECharts | 5.5+ | 数据可视化图表库 |
| Day.js | 1.11+ | 轻量级日期处理库 |

---

## 📄 页面功能说明

### 1. 登录页 (`/login`)

**功能：**
- 管理员账号密码登录
- 登录状态持久化
- 自动跳转到首页

**默认账号：**
- 账号：`admin`
- 密码：`admin123`

---

### 2. 首页 - 数据统计 (`/dashboard`)

**功能：**
- 用户总数、活跃用户统计
- 下注总数、净利润统计
- 下注总额、手续费、总赢/总输统计
- 每日统计趋势图表
- 下注类型分布饼图
- 用户排行榜 Top 10

**操作：**
1. 选择日期范围查看统计数据
2. 查看图表分析趋势
3. 查看用户排行榜

---

### 3. 用户管理 (`/users`)

**功能：**
- 用户列表查看（用户名、昵称、积分、下注统计等）
- 搜索用户（用户名/昵称）
- 按状态筛选（正常/禁用/冻结）
- 创建新用户
- 调整用户积分
- 重置用户密码
- 管理用户状态（正常/禁用/冻结）

**操作步骤：**

#### 创建用户
1. 点击"创建用户"按钮
2. 填写用户名（3-20位）、密码（6-20位）、昵称、初始积分
3. 点击"确定"完成创建

#### 调整积分
1. 点击用户列表中的"调整积分"按钮
2. 输入调整金额（正数为增加，负数为减少）
3. 填写备注说明
4. 点击"确定"完成调整

#### 重置密码
1. 点击用户列表中的"重置密码"按钮
2. 输入新密码（6-20位）
3. 点击"确定"完成重置

#### 状态管理
1. 点击用户列表中的"状态管理"按钮
2. 选择新状态（正常/禁用/冻结）
3. 点击"确定"完成更新

---

### 4. 开奖历史 (`/lottery-history`)

**功能：**
- 查看开奖历史记录
- 按期号搜索
- 显示开奖号码、总和、大小、单双、组合结果
- 显示是否回本及回本原因
- 手动同步开奖数据

**操作：**
1. 输入期号进行搜索
2. 点击"同步开奖数据"获取最新数据
3. 查看开奖详情

---

### 5. 下单记录 (`/bet-records`)

**功能：**
- 查看所有下注记录
- 多条件筛选（用户ID、期号、下注类型、状态、日期范围）
- 查看下注详情（金额、手续费、结算金额等）
- 查看用户下注前后积分变化

**筛选条件：**
- 用户ID：精确查找指定用户的下注
- 期号：查找指定期号的下注
- 下注类型：倍数/组合
- 状态：待结算/赢/输
- 日期范围：按时间段筛选

---

### 6. 积分记录 (`/point-records`)

**功能：**
- 查看所有积分变动记录
- 多条件筛选（用户ID、类型、日期范围）
- 查看变动详情（金额、前后余额、备注、操作员等）

**积分类型：**
- 管理员充值
- 管理员扣除
- 下注扣除
- 手续费
- 下注返还
- 充值
- 提现

---

### 7. 模式设置 (`/bet-settings`)

**功能：**
- 倍数下注设置
  - 倍数手续费比例（%）
  - 倍数手续费基数
  - 不回本损失比例（0-1）
- 组合下注设置
  - 组合手续费比例（%）
  - 组合手续费基数
- 通用设置
  - 最小下注金额
  - 最大下注金额
  - 单期最大下注次数

**操作：**
1. 修改相应参数
2. 点击"保存设置"
3. 点击"重置"恢复原始值

**说明：**
- 倍数手续费 = 下注金额 × 倍数 / 手续费基数 × 手续费比例
- 组合手续费 = 下注金额 / 手续费基数 × 手续费比例
- 基数越大，手续费越低

---

### 8. 网站设置 (`/system-settings`)

**功能：**
- 游戏控制
  - 游戏开关（关闭后用户无法下注）
  - 维护模式（开启后用户无法访问）
  - 自动结算开关
- 系统公告
  - 编辑系统公告内容（最多500字）
- 数据源设置
  - 开奖数据源URL配置

**操作：**
1. 修改相应设置
2. 点击"保存设置"
3. 点击"重置"恢复原始值

---

## 🔐 权限说明

- **未登录：** 只能访问登录页
- **已登录：** 可以访问所有管理功能
- **Token 过期：** 自动跳转到登录页

---

## 🌐 API 接口配置

### 开发环境
```
baseURL: /api
proxy: http://localhost:3000
```

### 生产环境
```
baseURL: /api
target: https://api.example.com
```

可以在以下文件中修改：
- `vite.config.ts` - 开发代理配置
- `.env.development` - 开发环境变量（无法编辑）
- `.env.production` - 生产环境变量（无法编辑）

---

## 🎨 自定义配置

### 1. 修改端口

在 `vite.config.ts` 中修改：

```typescript
server: {
  port: 5174, // 修改为你想要的端口
}
```

### 2. 修改代理

在 `vite.config.ts` 中修改：

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:3000', // 修改为你的后端地址
      changeOrigin: true,
    },
  },
}
```

### 3. 添加新路由

在 `src/router/index.ts` 中添加：

```typescript
{
  path: 'your-page',
  name: 'YourPage',
  component: () => import('@/views/YourPage.vue'),
  meta: { title: '页面标题', icon: '图标名称' },
}
```

### 4. 添加新 API

在 `src/api/` 目录下创建新文件或在现有文件中添加：

```typescript
export const yourApi = (params: any) => {
  return request.get('/your-endpoint', { params })
}
```

---

## 📦 打包部署

### 1. 打包

```bash
npm run build
```

生成的文件在 `dist/` 目录

### 2. 部署到 Nginx

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        root /path/to/dist;
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://backend-server:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 3. 部署到 Apache

在 `dist/.htaccess` 中添加：

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

---

## 🐛 常见问题

### 1. 安装依赖失败

**解决方案：**
```bash
# 清除缓存
npm cache clean --force

# 重新安装
npm install --legacy-peer-deps
```

### 2. 启动失败

**检查：**
- Node.js 版本是否 >= 18
- 端口 5174 是否被占用
- 依赖是否完整安装

### 3. API 请求失败

**检查：**
- 后端服务是否启动
- API 地址是否正确
- 网络是否正常
- Token 是否过期

### 4. 页面空白

**解决方案：**
- 打开浏览器控制台查看错误
- 检查路由配置
- 检查组件是否正确导入

---

## 📚 相关文档

- [Vue 3 官方文档](https://vuejs.org/)
- [TypeScript 文档](https://www.typescriptlang.org/)
- [Element Plus 文档](https://element-plus.org/)
- [Vue Router 文档](https://router.vuejs.org/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [ECharts 文档](https://echarts.apache.org/)
- [Vite 文档](https://vitejs.dev/)

---

## 💡 开发建议

### 1. 代码规范

- 使用 TypeScript 类型注解
- 组件命名使用 PascalCase
- 文件命名使用 kebab-case
- 变量命名使用 camelCase
- 常量命名使用 UPPER_SNAKE_CASE

### 2. 组件设计

- 保持组件单一职责
- 合理拆分大组件
- 使用 Composition API
- 提取公共逻辑到 composables

### 3. 性能优化

- 使用 `v-show` 代替频繁切换的 `v-if`
- 列表渲染添加唯一 `key`
- 大数据列表使用虚拟滚动
- 路由懒加载
- 组件按需导入

### 4. 错误处理

- 统一在 axios 拦截器中处理
- 用户友好的错误提示
- 记录详细的错误日志
- 提供错误恢复机制

---

## 📝 更新日志

### v1.0.0 (2024-11-26)

**初始版本**
- ✅ 完整的管理后台界面
- ✅ 用户管理功能
- ✅ 开奖历史查询
- ✅ 下注记录管理
- ✅ 积分记录查询
- ✅ 系统设置功能
- ✅ 数据统计和图表
- ✅ 响应式设计
- ✅ TypeScript 类型支持
- ✅ 完善的错误处理

---

## 👨‍💻 开发团队

如有问题或建议，请联系开发团队。

---

**祝你使用愉快！🎉**




