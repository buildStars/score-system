# 多数据源自动切换方案 🔄

## 📋 方案概述

本系统已实现**多数据源自动故障转移**架构，确保开奖数据的高可用性。当主数据源失效时，系统会自动切换到备用数据源，最大程度保障系统稳定运行。

### 🎯 核心优势

- ✅ **零停机切换**: 数据源失败时自动切换，无需人工干预
- ✅ **多层备份**: 3个数据源（2个API + 1个数据库）
- ✅ **优先级管理**: 按优先级自动选择最优数据源
- ✅ **健康监控**: 实时监控各数据源状态
- ✅ **统一接口**: 所有数据源使用统一的数据格式

---

## 🏗️ 架构设计

### 数据源列表

| 优先级 | 数据源名称 | API地址 | 状态 | 说明 |
|-------|----------|---------|-----|------|
| 1 | **USA28** | `https://api.365kaik.com/api/v1/trend/getHistoryList` | ✅ 可用 | 主数据源 |
| 2 | **JND28** | `https://c2api.canada28.vip/api/lotteryresult/result_jnd28` | ✅ 新增 | 备用数据源1（加拿大28）|
| 99 | **Database** | 本地数据库 | ✅ 可用 | 备用数据源2（兜底） |

### 工作流程

```
┌─────────────┐
│ 前端请求    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────┐
│  LotterySyncService         │ ← 定时任务触发同步
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│ LotteryDataSourceManager    │ ← 数据源管理器
└──────┬──────────────────────┘
       │
       ├─────────────┬─────────────┬──────────────┐
       ▼             ▼             ▼              ▼
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐
│ USA28    │  │ JND28    │  │ Database │  │ 未来... │
│ 优先级:1 │  │ 优先级:2 │  │ 优先级:99│  │         │
└──────────┘  └──────────┘  └──────────┘  └─────────┘
       │             │             │
       └─────────────┴─────────────┴────────┐
                                             ▼
                                    ┌────────────────┐
                                    │ 统一数据格式   │
                                    └────────┬───────┘
                                             ▼
                                    ┌────────────────┐
                                    │ 保存到数据库   │
                                    └────────────────┘
```

---

## 📝 技术实现

### 1. 数据源接口定义

```typescript
// lottery-data-source.interface.ts
export interface ILotteryDataSource {
  name: string;          // 数据源名称
  priority: number;      // 优先级（数字越小优先级越高）
  enabled: boolean;      // 是否启用
  fetchLatestData(): Promise<LotteryDataItem[]>;  // 获取最新数据
  testConnection(): Promise<boolean>;             // 测试连接
}

// 统一的数据格式
export interface LotteryDataItem {
  issue: string;         // 期号
  drawTime: Date;        // 开奖时间
  number1: number;       // 第一个号码
  number2: number;       // 第二个号码
  number3: number;       // 第三个号码
  sumValue: number;      // 和值
  source: string;        // 数据来源
}
```

### 2. 数据源实现

#### USA28数据源（主数据源）

```typescript
// usa28.data-source.ts
@Injectable()
export class USA28DataSource implements ILotteryDataSource {
  name = 'USA28';
  priority = 1;
  enabled = true;
  
  async fetchLatestData(): Promise<LotteryDataItem[]> {
    const response = await axios.get('https://api.365kaik.com/api/v1/trend/getHistoryList', {
      params: { lotCode: '10029', pageSize: '2', pageNum: '0' },
      timeout: 10000,
    });
    
    // 解析USA28格式并转换为统一格式
    return response.data.data.list.map(item => ({
      issue: item.drawIssue,
      drawTime: new Date(item.drawTime),
      number1: parseInt(item.drawCode.split(',')[0]),
      number2: parseInt(item.drawCode.split(',')[1]),
      number3: parseInt(item.drawCode.split(',')[2]),
      sumValue: ...,
      source: 'USA28',
    }));
  }
}
```

#### JND28数据源（备用数据源1）

```typescript
// jnd28.data-source.ts
@Injectable()
export class JND28DataSource implements ILotteryDataSource {
  name = 'JND28';
  priority = 2;
  enabled = true;
  
  async fetchLatestData(): Promise<LotteryDataItem[]> {
    const response = await axios.get('https://c2api.canada28.vip/api/lotteryresult/result_jnd28', {
      params: { game_id: '7', page: '1', pageSize: '2' },
      timeout: 10000,
    });
    
    // 解析JND28格式并转换为统一格式
    return response.data.result_list.map(item => ({
      issue: String(item.expect),
      drawTime: new Date(item.datetime),
      number1: Number(item.code1),
      number2: Number(item.code2),
      number3: Number(item.code3),
      sumValue: Number(item.he),
      source: 'JND28',
    }));
  }
}
```

#### Database数据源（兜底）

```typescript
// database.data-source.ts
@Injectable()
export class DatabaseDataSource implements ILotteryDataSource {
  name = 'Database';
  priority = 99;  // 最低优先级
  enabled = true;
  
  async fetchLatestData(): Promise<LotteryDataItem[]> {
    // 从数据库读取最新2条记录
    const results = await this.prisma.lotteryResult.findMany({
      orderBy: { drawTime: 'desc' },
      take: 2,
    });
    
    return results.map(item => ({
      issue: item.issue,
      drawTime: item.drawTime,
      number1: item.number1,
      number2: item.number2,
      number3: item.number3,
      sumValue: item.resultSum,
      source: 'Database',
    }));
  }
}
```

### 3. 数据源管理器

```typescript
// lottery-data-source.manager.ts
@Injectable()
export class LotteryDataSourceManager {
  private dataSources: ILotteryDataSource[] = [];

  constructor(
    private usa28Source: USA28DataSource,
    private jnd28Source: JND28DataSource,
    private databaseSource: DatabaseDataSource,
  ) {
    // 按优先级排序
    this.dataSources = [
      this.usa28Source,
      this.jnd28Source,
      this.databaseSource,
    ].sort((a, b) => a.priority - b.priority);
  }

  /**
   * 自动故障转移获取数据
   */
  async fetchLatestData(): Promise<DataSourceResult> {
    const enabledSources = this.dataSources.filter(s => s.enabled);
    
    // 按优先级依次尝试
    for (const source of enabledSources) {
      try {
        logger.log(`🔍 尝试数据源: ${source.name} (优先级: ${source.priority})`);
        
        const data = await source.fetchLatestData();
        
        if (data && data.length > 0) {
          logger.log(`✅ 成功从 ${source.name} 获取 ${data.length} 条数据`);
          return { success: true, data, source: source.name };
        }
      } catch (error) {
        logger.error(`❌ ${source.name} 失败: ${error.message}`);
        continue;  // 继续尝试下一个数据源
      }
    }
    
    // 所有数据源都失败
    return { success: false, error: '所有数据源均失败', source: 'none' };
  }

  /**
   * 健康检查
   */
  async healthCheck() {
    const result = {
      timestamp: new Date(),
      status: 'healthy',
      dataSources: [],
    };
    
    for (const source of this.dataSources) {
      try {
        const isHealthy = await source.testConnection();
        result.dataSources.push({
          name: source.name,
          priority: source.priority,
          enabled: source.enabled,
          status: isHealthy ? 'ok' : 'failed',
        });
      } catch (error) {
        result.dataSources.push({
          name: source.name,
          priority: source.priority,
          enabled: source.enabled,
          status: 'failed',
          error: error.message,
        });
      }
    }
    
    return result;
  }
}
```

---

## 🚀 使用方式

### 后端同步

```typescript
// lottery.service.ts
async syncLotteryData() {
  // 1. 使用数据源管理器自动获取数据
  const result = await this.dataSourceManager.fetchLatestData();
  
  if (!result.success || !result.data) {
    throw new Error('所有数据源均失败，无法获取开奖数据');
  }
  
  logger.log(`✅ 成功从 ${result.source} 获取 ${result.data.length} 条数据`);
  
  // 2. 保存到数据库
  for (const item of result.data) {
    await this.prisma.lotteryResult.create({
      data: {
        issue: item.issue,
        number1: item.number1,
        number2: item.number2,
        number3: item.number3,
        resultSum: item.sumValue,
        drawTime: item.drawTime,
        // ...其他字段
      },
    });
  }
  
  return {
    message: `成功同步 ${result.data.length} 条数据 (来源: ${result.source})`,
    dataSource: result.source,
  };
}
```

### 健康检查接口

```http
GET /api/lottery/data-source-health
Authorization: Bearer <admin_token>

Response:
{
  "timestamp": "2025-11-27T16:00:00.000Z",
  "status": "healthy",
  "dataSources": [
    {
      "name": "USA28",
      "priority": 1,
      "enabled": true,
      "status": "ok",
      "responseTime": 245
    },
    {
      "name": "JND28",
      "priority": 2,
      "enabled": true,
      "status": "ok",
      "responseTime": 312
    },
    {
      "name": "Database",
      "priority": 99,
      "enabled": true,
      "status": "ok",
      "responseTime": 5
    }
  ]
}
```

---

## 📊 故障转移示例

### 场景1：USA28正常

```
[INFO] 🔍 尝试数据源: USA28 (优先级: 1)
[INFO] ✅ 成功从 USA28 获取 2 条数据 (245ms)
[INFO] ✓ 成功保存期号 3365450: 3+9+0=12 (来源: USA28)
[INFO] ✓ 成功保存期号 3365449: 0+2+1=3 (来源: USA28)
```

### 场景2：USA28失败，自动切换到JND28

```
[INFO] 🔍 尝试数据源: USA28 (优先级: 1)
[ERROR] ❌ USA28 失败 (10000ms): connect ETIMEDOUT
[INFO] ➡️ 切换到下一个数据源...
[INFO] 🔍 尝试数据源: JND28 (优先级: 2)
[INFO] ✅ 成功从 JND28 获取 2 条数据 (312ms)
[INFO] ✓ 成功保存期号 3365450: 3+9+0=12 (来源: JND28)
[INFO] ✓ 成功保存期号 3365449: 0+2+1=3 (来源: JND28)
```

### 场景3：所有API失败，使用数据库兜底

```
[INFO] 🔍 尝试数据源: USA28 (优先级: 1)
[ERROR] ❌ USA28 失败: Network timeout
[INFO] ➡️ 切换到下一个数据源...
[INFO] 🔍 尝试数据源: JND28 (优先级: 2)
[ERROR] ❌ JND28 失败: Connection refused
[INFO] ➡️ 切换到下一个数据源...
[INFO] 🔍 尝试数据源: Database (优先级: 99)
[INFO] ✅ 成功从 Database 获取 2 条数据 (5ms)
[INFO] ℹ️ 使用数据库缓存数据（所有API暂时不可用）
```

---

## 🔧 配置管理

### 启用/禁用数据源

```typescript
// 禁用USA28
dataSourceManager.setDataSourceEnabled('USA28', false);

// 启用JND28
dataSourceManager.setDataSourceEnabled('JND28', true);
```

### 调整优先级

修改数据源类的 `priority` 属性：

```typescript
export class JND28DataSource implements ILotteryDataSource {
  name = 'JND28';
  priority = 1;  // 改为优先级1（原来是2）
  enabled = true;
}
```

---

## 🎯 最佳实践

### 1. 监控和告警

定期检查数据源健康状态：

```typescript
// 每5分钟检查一次
@Cron('*/5 * * * *')
async monitorDataSources() {
  const health = await this.dataSourceManager.healthCheck();
  
  if (health.status === 'critical') {
    // 发送告警：所有数据源失败
    await this.alertService.sendCritical('所有数据源失效');
  } else if (health.status === 'degraded') {
    // 发送警告：部分数据源失败
    await this.alertService.sendWarning('部分数据源失效');
  }
}
```

### 2. 日志记录

记录每次数据源切换：

```typescript
logger.log({
  event: 'data_source_switch',
  from: 'USA28',
  to: 'JND28',
  reason: 'Connection timeout',
  timestamp: new Date(),
});
```

### 3. 性能优化

- 设置合理的超时时间（10秒）
- 只获取必要的数据量（pageSize=2）
- 使用连接池复用
- 添加本地缓存

---

## 📈 未来扩展

### 添加新数据源

1. 创建新的数据源类：

```typescript
// pc28.data-source.ts
@Injectable()
export class PC28DataSource implements ILotteryDataSource {
  name = 'PC28';
  priority = 3;
  enabled = true;
  
  async fetchLatestData(): Promise<LotteryDataItem[]> {
    // 实现获取逻辑
  }
}
```

2. 在 `lottery.module.ts` 中注册：

```typescript
providers: [
  // ...
  PC28DataSource,
]
```

3. 在 `LotteryDataSourceManager` 中添加：

```typescript
constructor(
  // ...
  private pc28Source: PC28DataSource,
) {
  this.dataSources = [
    this.usa28Source,
    this.jnd28Source,
    this.pc28Source,  // 新增
    this.databaseSource,
  ].sort((a, b) => a.priority - b.priority);
}
```

### 数据源配置持久化

将数据源配置保存到数据库：

```prisma
model LotteryDataSource {
  id          Int      @id @default(autoincrement())
  name        String   @unique
  url         String
  priority    Int      @default(1)
  enabled     Boolean  @default(true)
  lastSuccess DateTime?
  lastFailure DateTime?
  failureCount Int     @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

---

## 🚨 故障排查

### 问题1：所有数据源都失败

**现象**：
```
❌ USA28 失败: Network timeout
❌ JND28 失败: Connection refused
❌ Database 失败: No data available
🚨 所有数据源均失败
```

**解决方案**：
1. 检查网络连接
2. 检查防火墙设置
3. 验证API地址是否变更
4. 查看数据库是否有历史数据
5. 考虑手动录入数据

### 问题2：数据源频繁切换

**现象**：
```
✅ 成功从 USA28 获取数据
❌ USA28 失败
✅ 成功从 JND28 获取数据
❌ JND28 失败
```

**解决方案**：
1. 增加超时时间
2. 添加重试机制
3. 检查网络稳定性
4. 考虑使用备用网络

---

## 📞 技术支持

- 查看日志：`score-system/backend/logs/`
- 监控面板：管理后台 > 系统监控 > 数据源健康
- API文档：`http://localhost:3000/api`

---

**文档版本**: v1.0  
**最后更新**: 2025-11-29  
**相关文档**: [数据源风险与备份方案.md](./数据源风险与备份方案.md)

